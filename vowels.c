#include<stdio.h>
#include<conio.h>
void main()
{ 
char ch;
printf("Enter any characters");
scanf("%&ch);
if(ch=='a'||ch=='e'||ch=='i'||ch=='o'||ch=='u')
{
printf("enter character is vowels");
else
{
printf("enrer the character is consonant");
}
return 0;
}
