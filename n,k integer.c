#include<stdio.h>
#include<conio.h>
int main()
{
int array[25],limt,a,b;
printf("enter the array"\n):
scanf("%d",&limit);
printf("enter the"%d" element",limit);
for(a=0;a<limit;a++)
{
scanf("%d",array[a]);
}
printf("The element is\n");
{
for(b=0;b<limit;b++)
{
if(array[a]=array[b])
break;
}
if(a==b)
{
printf("%d",array[a]);
}
}
getch()
return 0
}
