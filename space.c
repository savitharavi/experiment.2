#include<stdio.h>
#include<string.h>
void main()
{
	int add=0 ,c;
	char str[100];
  printf("Enter the string");
  gets(str);
	c=strlen(str);
	for(int i=0;i<=a;i++)
	{
		if(str[i]==' ')
		{
		    add=add+1;
		}
	}
	printf("The number of space in the line is %d\n",add);
}
