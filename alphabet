#include<stdio.h>
#include<conio.h>
void main()
{ 
char ch();
printf ("Enter any alphabets");
scanf("%c&ch);
if((ch>=65)&&(ch=90)||(ch>=97)&&(ch<=122))
{
printf("enter the character is alphabet");
}
else
}
